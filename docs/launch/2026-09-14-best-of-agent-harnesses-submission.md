# Best of Agent Harnesses submission draft

Status: **draft only — do not submit without a separate external-posting authorization**

Target: [RyanAlberts/best-of-Agent-Harnesses](https://github.com/RyanAlberts/best-of-Agent-Harnesses)

The target contribution guide says the fastest route is an issue containing the repository
URL, one sentence describing the harness contribution, and a category. Minimal Harness is
new and has no independent adoption signal yet, so the honest expected destination is the
project's **On the radar** section rather than its ranked list.

## Issue title

```text
Add project: Minimal Harness
```

## Issue body

```markdown
Repository: https://github.com/2278091160dg-rgb/minimal-harness

Harness: Minimal Harness is a repository-local, dependency-free acceptance and handoff
kernel: it runs configured checks, binds evidence to the frozen task contract and current
Git subject, and fails closed when that proof becomes stale, while the user's existing
coding agent continues to own planning and execution.

Suggested category: Coding harness configs and SDKs

Current status: MIT, Python 3.9+ standard-library runtime, schema v3 prerelease. The
project is new and would be appropriate for “On the radar” while external adoption is
still being measured.

Show it in action:
https://github.com/2278091160dg-rgb/minimal-harness#try-a-complete-source-example
```

## Pre-submission checks

- Confirm the public README still uses the same product boundary and quickstart anchor.
- Confirm the GitHub About description matches the README positioning.
- Re-read the target contribution guide because its categories and curation bar may change.
- Search the target repository again for `minimal-harness` and the full repository URL.
- Submit only after the owner explicitly authorizes posting to the external repository.
- If accepted only to **On the radar**, do not describe that as a ranked recommendation.
- Add the target's listing badge only after a listing actually exists.
